using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class AuswahlSceneManager : MonoBehaviour
{
   public void LoadFishScene(string sceneName)
    {
        SceneManager.LoadScene(sceneName);
    }
    public void LoadFisherScene(string sceneName)
    {
        SceneManager.LoadScene(sceneName);
    }
}
